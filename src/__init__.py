"""E2ER v3 — End-to-End Researcher."""

# Single source of truth for the package version. Must match pyproject.toml
# [project].version — enforced by scripts/release_audit.py and the
# version-check job in .github/workflows/release.yml.
__version__ = "0.4.2"
